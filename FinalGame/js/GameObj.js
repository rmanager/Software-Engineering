function GameObj(program, x, y)  {

    this.x = x;
    this.y = y;

    /*GameObject.prototype.move = function (speed) {		// Pass in negative speed for backward motion
	this.x = this.x + speed * this.xdir;
	this.z = this.z + speed * this.zdir;
    };*/
};

function getX()  {

    return this.x = x;
};